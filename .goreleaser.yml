version: 2
project_name: reglint

before:
  hooks:
    - go mod tidy

builds:
  - id: reglint
    main: ./cmd/reglint
    binary: reglint
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/iyaki/reglint/internal/cli.version={{ .Version }}

archives:
  - id: reglint
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: checksums.txt

changelog:
  use: git
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"
      - "^refactor:"
      - "Merge branch"

release:
  draft: false
  prerelease: auto
